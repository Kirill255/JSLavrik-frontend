Переписать всё api с коллбэков на promise или async/await.

Переписывать нужно всё кроме приватной и полуприватной части из файла db.js, но функцию function TimeoutPropabiliry() из полуприватной части, лучше тоже переписать, т.к. будет проще организовывать код. И даже лучше начать рефакторить код именно с этой функции.
